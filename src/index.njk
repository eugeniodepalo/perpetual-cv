<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <link rel="icon" type="image/svg+xml" href="/favicon.ico"/>
    <link rel="stylesheet" href="/index.css"/>
    <title>{{ name }}</title>
  </head>
  <body class="bg-background text-content max-w-screen-xl mx-auto p-6 md:p-8">
    <header class="mb-12">
      <div class="flex flex-col md:flex-row md:items-center md:justify-between">
        <div>
          <h1 class="text-4xl font-bold text-primary">{{ name }}</h1>
          <p class="text-xl text-secondary mt-2">{{ title }}</p>
        </div>
        <div class="mt-4 md:mt-0">
          <nav class="flex flex-col sm:flex-row gap-2 sm:gap-4" aria-label="Contact information">
            <a href="mailto:{{ email }}" class="hover:text-accent flex items-center gap-1">
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-mail-icon lucide-mail"><rect width="20" height="16" x="2" y="4" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/></svg>
              Email
            </a>
            <a href="https://github.com/{{ github }}" class="hover:text-accent flex items-center gap-1">
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-github-icon lucide-github"><path d="M15 22v-4a4.8 4.8 0 0 0-1-3.5c3 0 6-2 6-5.5.08-1.25-.27-2.48-1-3.5.28-1.15.28-2.35 0-3.5 0 0-1 0-3 1.5-2.64-.5-5.36-.5-8 0C6 2 5 2 5 2c-.3 1.15-.3 2.35 0 3.5A5.403 5.403 0 0 0 4 9c0 3.5 3 5.5 6 5.5-.39.49-.68 1.05-.85 1.65-.17.6-.22 1.23-.15 1.85v4"/><path d="M9 18c-4.51 2-5-2-7-2"/></svg>
              GitHub
            </a>
            <a href="https://linkedin.com/in/{{ linkedin }}" class="hover:text-accent flex items-center gap-1">
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-linkedin-icon lucide-linkedin"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect width="4" height="12" x="2" y="9"/><circle cx="4" cy="4" r="2"/></svg>
              LinkedIn
            </a>
          </nav>
        </div>
      </div>
      <div class="mt-6">
        <p class="mb-2">{{ description1 }}</p>
        <p class="mb-2">{{ description2 }}</p>
      </div>
    </header>
    <main>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <aside class="md:col-span-1">
          <section class="mb-8" aria-labelledby="skills-heading">
            <h2 id="skills-heading" class="text-xl font-semibold mb-3 text-primary border-b border-divider pb-1">Skills</h2>
            <ul class="flex flex-wrap gap-2">
              {% for skill in skills %}
                <li class="bg-accent/10 text-accent px-2 py-1 rounded-md text-sm">{{ skill }}</li>
              {% endfor %}
            </ul>
          </section>
          <section class="mb-8" aria-labelledby="learning-heading">
            <h2 id="learning-heading" class="text-xl font-semibold mb-3 text-primary border-b border-divider pb-1">Currently Learning</h2>
            <ul class="flex flex-wrap gap-2">
              {% for item in learning %}
                <li class="bg-secondary/10 text-secondary px-2 py-1 rounded-md text-sm">{{ item }}</li>
              {% endfor %}
            </ul>
          </section>
          <section class="mb-8" aria-labelledby="languages-heading">
            <h2 id="languages-heading" class="text-xl font-semibold mb-3 text-primary border-b border-divider pb-1">Languages</h2>
            <ul class="list-disc pl-5 space-y-1">
              {% for language in languages %}
                <li>{{ language }}</li>
              {% endfor %}
            </ul>
          </section>
          <section class="mb-8" aria-labelledby="interests-heading">
            <h2 id="interests-heading" class="text-xl font-semibold mb-3 text-primary border-b border-divider pb-1">Interests</h2>
            <ul class="list-disc pl-5 space-y-1">
              {% for interest in interests %}
                <li>{{ interest }}</li>
              {% endfor %}
            </ul>
          </section>
          <section class="mb-8" aria-labelledby="education-heading">
            <h2 id="education-heading" class="text-xl font-semibold mb-3 text-primary border-b border-divider pb-1">Education</h2>
            {% for edu in education %}
              <div class="mb-3">
                <h3 class="font-medium">{{ edu.institution }}</h3>
                <p class="text-sm">{{ edu.degree }}</p>
              </div>
            {% endfor %}
          </section>
          <section aria-labelledby="projects-heading">
            <h2 id="projects-heading" class="text-xl font-semibold mb-3 text-primary border-b border-divider pb-1">Projects</h2>
            {% for project in projects %}
              <div class="mb-4">
                <h3 class="font-medium">
                  {% if project.url %}
                    <a href="{{ project.url }}" class="hover:text-accent">{{ project.name }}</a>
                  {% else %}
                    {{ project.name }}
                  {% endif %}
                </h3>
                <p class="text-sm">{{ project.description }}</p>
              </div>
            {% endfor %}
          </section>
        </aside>
        <section class="md:col-span-2" aria-labelledby="experience-heading">
          <h2 id="experience-heading" class="text-2xl font-semibold mb-6 text-primary border-b border-divider pb-2">Work Experience</h2>
          <div class="space-y-8">
            {% for job in experience %}
              <article class="pb-6 border-b border-divider last:border-0">
                <header class="mb-3">
                  <div class="flex flex-col sm:flex-row sm:items-center justify-between">
                    <h3 class="text-lg font-semibold">
                      {% if job.url %}
                        <a href="{{ job.url }}" class="hover:text-accent">{{ job.company }}</a>
                      {% else %}
                        {{ job.company }}
                      {% endif %}
                    </h3>
                    <span class="text-sm text-secondary">{{ job.date }}</span>
                  </div>
                  <p class="text-md font-medium">{{ job.role }}</p>
                </header>
                <div class="mb-3">
                  <h4 class="sr-only">Technologies used</h4>
                  <ul class="flex flex-wrap gap-2 mb-3">
                    {% for tech in job.technologies %}
                      <li class="bg-primary/10 text-primary px-2 py-0.5 text-xs rounded-md">{{ tech }}</li>
                    {% endfor %}
                  </ul>
                </div>
                <div>
                  <h4 class="sr-only">Key achievements</h4>
                  <ul class="list-disc pl-5 space-y-1">
                    {% for achievement in job.achievements %}
                      <li>{{ achievement }}</li>
                    {% endfor %}
                  </ul>
                </div>
              </article>
            {% endfor %}
          </div>
        </section>
      </div>
    </main>
    <footer class="mt-12 pt-6 border-t border-divider text-center text-sm text-secondary">
      <p class="mb-2">Like the format of this CV? <a href="https://github.com/eugeniodepalo/perpetual-cv" class="hover:text-accent">Fork it on GitHub</a>.
        <p>Made with ❤️ by {{ name }}.</p>
      </footer>
    </body>
  </html>
