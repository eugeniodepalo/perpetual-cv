<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.ico" />
    <link rel="stylesheet" href="/index.css" />
    <title>{{ name }}</title>
  </head>
  <body class="bg-gray-100 font-mono">

<a href="#main-content" class="absolute -left-full focus:left-4 focus:top-4 z-50 p-4 bg-white text-black font-bold no-underline border-2 border-gray-700 focus:outline-none focus:block">Skip to main content</a>
<header class="sticky top-0 bg-white shadow-md z-10" role="banner">
  <div class="container mx-auto px-4 py-3 flex items-center justify-between">
    <div class="flex items-center">
      <div class="w-16 h-16 rounded-full overflow-hidden mr-4">
        <img
          src="/images/avatar.webp"
          alt="Profile photo"
          class="w-full h-full object-cover"
        />
      </div>
      <h1 class="text-2xl font-bold">{{ name }}</h1>
    </div>
    <nav aria-label="Social links" class="flex items-center space-x-4">
      <a
        href="https://github.com/{{ github }}"
        target="_blank"
        class="hover:bg-secondary p-2 rounded-full"
        aria-label="{{ name }}'s GitHub profile"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-github" aria-hidden="true"><path d="M15 22v-4a4.8 4.8 0 0 0-1-3.5c3 0 6-2 6-5.5.08-1.25-.27-2.48-1-3.5.28-1.15.28-2.35 0-3.5 0 0-1 0-3 1.5-2.64-.5-5.36-.5-8 0C6 2 5 2 5 2c-.3 1.15-.3 2.35 0 3.5A5.403 5.403 0 0 0 4 9c0 3.5 3 5.5 6 5.5-.39.49-.68 1.05-.85 1.65-.17.6-.22 1.23-.15 1.85v4"/><path d="M9 18c-4.51 2-5-2-7-2"/></svg>
      </a>
      <a
        href="mailto:{{ email }}"
        class="hover:bg-secondary p-2 rounded-full"
        aria-label="Send email to {{ name }}"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-mail" aria-hidden="true"><rect width="20" height="16" x="2" y="4" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/></svg>
      </a>
    </nav>
  </div>
</header>
<main id="main-content" class="container mx-auto px-4 py-8" role="main">
  <article class="bg-white rounded-lg shadow-md mb-8 p-6">
    <h2 class="text-3xl mb-4">{{ title }}</h2>
    <p class="text-gray-700 text-2xl mb-4">{{ description1 }}</p>
    <p class="text-gray-700 text-2xl">{{ description2 }}</p>
    
    <section class="mt-8 pt-6 border-t border-dashed border-gray-300" aria-labelledby="skills-heading">
      <h3 id="skills-heading" class="text-lg font-medium mb-4">My Skills</h3>
      <ul class="flex flex-wrap gap-2 list-none pl-0">
        {% for skill in skills %}
          <li class="bg-primary px-3 py-1 rounded-full text-sm">{{ skill }}</li>
        {% endfor %}
      </ul>
    </section>
    <section class="mt-8 pt-6 border-t border-dashed border-gray-300" aria-labelledby="experience-heading">
      <h3 id="experience-heading" class="text-lg font-medium mb-4">My Experience</h3>
      <div class="space-y-12">
        {% for job in experience %}
          <article class="flex items-start justify-center">
            <time class="w-1/3 text-right pr-6 text-gray-600">{{ job.date }}</time>
            <div class="relative w-1/3 pl-6">
              <div
                class="absolute left-0 top-2 w-3 h-3 rounded-full {% if loop.first %}bg-secondary{% else %}bg-primary{% endif %}" 
                aria-hidden="true"
              ></div>
              <div
                {% if not loop.last %}
                class="absolute left-1.5 top-5 w-0.5 h-32 bg-gray-300"
                {% endif %}
                aria-hidden="true"
              ></div>
              {% if job.url %}
                <a
                  href="{{ job.url }}"
                  target="_blank"
                  class="block text-lg font-medium hover:text-secondary"
                  rel="noopener"
                >{{ job.company }}</a>
              {% else %}
                <h4 class="text-lg font-medium">{{ job.company }}</h4>
              {% endif %}
              <p class="text-gray-600 mt-2">{{ job.role }}</p>
            </div>
          </article>
        {% endfor %}
      </div>
    </section>
    <section class="mt-8 pt-6 border-t border-dashed border-gray-300" aria-labelledby="clients-heading">
      <h3 id="clients-heading" class="text-lg font-medium mb-4">My Clients</h3>
      <ul class="grid grid-cols-1 md:grid-cols-4 gap-4 list-none pl-0">
        {% for client in clients %}
          <li>
            <a
              href="{{ client.url }}"
              target="_blank"
              class="bg-primary hover:bg-secondary transition-colors duration-300 rounded-lg p-4 flex items-center justify-center h-32"
              aria-label="Visit {{ client.name }} website"
              rel="noopener"
            >
              <div class="relative h-20 w-full">
                <img
                  src="{{ client.logo }}"
                  alt="{{ client.name }} logo"
                  class="absolute inset-0 h-full w-full object-contain"
                />
              </div>
            </a>
          </li>
        {% endfor %}
      </ul>
    </section>
  </article>
</main>
<footer class="container mx-auto px-4 py-8 text-center" role="contentinfo">
  <p class="text-gray-600 mb-2">
    Like the format of this CV? Fork it on
    <a
      href="https://github.com/eugeniodepalo/perpetual-cv"
      class="hover:text-secondary underline"
      aria-label="Fork this CV template on GitHub"
      rel="noopener"
      >GitHub</a
    >
  </p>
  <p class="text-gray-600">Made with <span aria-hidden="true">❤️</span><span class="sr-only">love</span> by {{ name }}</p>
</footer>
  </body>
</html>
